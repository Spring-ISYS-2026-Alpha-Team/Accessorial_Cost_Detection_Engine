--extra-index-url https://download.pytorch.org/whl/cpu
streamlit>=1.31.0
pandas
numpy
torch>=2.0.0
plotly
scikit-learn
lightgbm
joblib
requests
python-dotenv
openpyxl

# Azure SQL (requires freetds — see packages.txt for system deps)
pymssql

# Document parsing
pdfplumber
Pillow
pytesseract
# sentence-transformers — optional; column_mapper + doc_parser degrade gracefully without it

# Geospatial routing
geopy
openrouteservice
